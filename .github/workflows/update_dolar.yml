name: "Actualizar precio del dólar"

on:
  schedule:
    # Bolivia es UTC-4 — 5 corridas diarias en días hábiles
    - cron: "0 14 * * 1-5"   # 10:00 BOT
    - cron: "0 17 * * 1-5"   # 13:00 BOT
    - cron: "0 20 * * 1-5"   # 16:00 BOT
    - cron: "0 23 * * 1-5"   # 19:00 BOT
    - cron: "30 3 * * 2-6"   # 23:30 BOT (mar-sáb = cierre lun-vie)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Ejecutar scraper
        run: python scripts/scrape_dolar.py

      - name: Commit y push si hay datos nuevos
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/dolar.csv data/historico.json data/bancos.json
          if git diff --cached --quiet; then
            echo "Sin datos nuevos."
          else
            git commit -m "datos: dólar $(date -u +%Y-%m-%d)"
            git push
          fi
